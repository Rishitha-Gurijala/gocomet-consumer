

CREATE TABLE drivers (id INT PRIMARY KEY, name VARCHAR(100) , latitude VARCHAR(100) , longitude VARCHAR(100) , is_available VARCHAR(100), password VARCHAR(100), users JSON);

ALTER TABLE drivers ADD COLUMN users JSON NOT NULL;
ALTER TABLE drivers ADD COLUMN is_available VARCHAR(100);
ALTER TABLE rides ADD COLUMN status VARCHAR(100);
ALTER TABLE rides ADD COLUMN driverId INT;
ALTER TABLE rides ADD COLUMN fare INT;


CREATE TABLE redisBackup ( regionNo INT PRIMARY KEY, users VARCHAR(1000) NOT NULL);

INSERT INTO drivers (id, name, latitude, longitude, users)VALUES (2222, "varma", 13.043020034575562, 77.67661329023345,  "[]");

UPDATE drivers SET users = JSON_ARRAY_APPEND(users, '$', '1234') WHERE id = 1111;
UPDATE drivers SET is_available = "true";
UPDATE rides SET fare = "true";

INSERT INTO redisBackup (regionNo, users)VALUES (2, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (3, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (4, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (5, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (6, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (7, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (8, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (9, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (10, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (11, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (12, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (13, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (14, "[]");
INSERT INTO redisBackup (regionNo, users)VALUES (15, "[]");
